{
  "openapi" : "3.1.0",
  "info" : {
    "title" : "SharingRule",
    "version" : "1.0.0"
  },
  "paths" : {
    "/api/v1/dataSharingRules" : {
      "get" : {
        "tags" : [ "SharingRule" ],
        "summary" : "Get Data Sharing Rules",
        "description" : "Retrieves the data sharing rules configured in the help desk portal.<p><b>Note:</b> These rules control user permissions to view or edit resources across modules in Zoho Desk.</p>",
        "operationId" : "getDataSharingRules",
        "parameters" : [ {
          "$ref" : "./Common.json#/components/parameters/orgId"
        } ],
        "responses" : {
          "200" : {
            "$ref" : "#/components/responses/sharingRuleResponse"
          }
        },
        "security" : [ {
          "iam-oauth2-schema" : [ "Desk.basic.READ", "Desk.permissions.READ", "Desk.settings.READ" ]
        } ],
        "x-audience" : [ "external-public" ],
        "x-mcp-ready" : true
      },
      "patch" : {
        "tags" : [ "SharingRule" ],
        "summary" : "Update Data Sharing Rules",
        "description" : "Updates the data sharing rules configured in the help desk portal.<p><b>Note:</b> These rules control user permissions to view or edit resources across modules in Zoho Desk.</p>",
        "operationId" : "updateDataSharingRules",
        "parameters" : [ {
          "$ref" : "./Common.json#/components/parameters/orgId"
        } ],
        "requestBody" : {
          "$ref" : "#/components/requestBodies/sharingRuleRequest"
        },
        "responses" : {
          "204" : {
            "$ref" : "./Common.json#/components/responses/emptyResponse"
          }
        },
        "security" : [ {
          "iam-oauth2-schema" : [ "Desk.permissions.UPDATE", "Desk.settings.UPDATE" ]
        } ],
        "x-audience" : [ "external-public" ],
        "x-mcp-ready" : true
      }
    }
  },
  "components" : {
    "schemas" : {
      "sharingRuleResponseSchema" : {
        "type" : [ "object", "null" ],
        "additionalProperties" : false,
        "properties" : {
          "tickets" : {
            "type" : [ "string", "null" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to tickets"
          },
          "calls" : {
            "type" : [ "string", "null" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to calls"
          },
          "accounts" : {
            "type" : [ "string", "null" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to accounts"
          },
          "contracts" : {
            "type" : [ "string", "null" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to SLA contracts"
          },
          "contacts" : {
            "type" : [ "string", "null" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to contacts"
          },
          "tasks" : {
            "type" : [ "string", "null" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to tasks"
          },
          "events" : {
            "type" : [ "string", "null" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to events"
          },
          "products" : {
            "type" : [ "string", "null" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to products"
          }
        },
        "required" : [ "accounts", "calls", "contacts", "contracts", "events", "products", "tasks", "tickets" ]
      },
      "sharingRuleRequestSchema" : {
        "type" : [ "object" ],
        "additionalProperties" : false,
        "properties" : {
          "tickets" : {
            "type" : [ "string" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to tickets"
          },
          "calls" : {
            "type" : [ "string" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to calls"
          },
          "accounts" : {
            "type" : [ "string" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to accounts"
          },
          "contracts" : {
            "type" : [ "string" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to SLA contracts"
          },
          "contacts" : {
            "type" : [ "string" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to contacts"
          },
          "tasks" : {
            "type" : [ "string" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to tasks"
          },
          "events" : {
            "type" : [ "string" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to events"
          },
          "products" : {
            "type" : [ "string" ],
            "enum" : [ "public", "private", "public read only" ],
            "description" : "Data sharing rule related to products"
          }
        }
      }
    },
    "responses" : {
      "sharingRuleResponse" : {
        "description" : "sharingRuleResponse template definitions",
        "content" : {
          "application/json" : {
            "schema" : {
              "$ref" : "#/components/schemas/sharingRuleResponseSchema"
            },
            "examples" : {
              "Valid responses Definitions" : {
                "value" : {
                  "tickets" : "public",
                  "calls" : "public read only",
                  "accounts" : "public read only",
                  "contracts" : "private",
                  "contacts" : "public",
                  "tasks" : "private",
                  "events" : "public",
                  "products" : "public"
                }
              }
            }
          }
        }
      }
    },
    "requestBodies" : {
      "sharingRuleRequest" : {
        "content" : {
          "application/json" : {
            "schema" : {
              "$ref" : "#/components/schemas/sharingRuleRequestSchema"
            },
            "examples" : {
              "Valid requestBody Definitions" : {
                "value" : {
                  "tickets" : "public",
                  "calls" : "public read only",
                  "accounts" : "public read only",
                  "contracts" : "private",
                  "contacts" : "public",
                  "tasks" : "private",
                  "events" : "public",
                  "products" : "public"
                }
              }
            }
          }
        }
      }
    },
    "securitySchemes" : {
      "cookie-auth" : {
        "$ref" : "./Common.json#/components/securitySchemes/cookie-auth"
      },
      "org-oauth" : {
        "$ref" : "./Common.json#/components/securitySchemes/org-oauth"
      },
      "iam-oauth2-schema" : {
        "$ref" : "./Common.json#/components/securitySchemes/iam-oauth2-schema"
      }
    }
  },
  "x-entity" : "SharingRule"
}